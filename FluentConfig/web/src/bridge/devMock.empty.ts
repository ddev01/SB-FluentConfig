import type { ChromeWebView } from './types';

/** Production stub — Vite aliases `devMock.ts` here so mock sources are excluded. */
export function tryGetMockWebView(): ChromeWebView | null {
  return null;
}
